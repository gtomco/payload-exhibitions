/** HttpOnly cookie set after successful kiosk PIN auth. */
export const VISITOR_CHECKIN_COOKIE = 'visitor-checkin'

/** Max age for kiosk session (8 hours). */
export const VISITOR_CHECKIN_TTL_SECONDS = 60 * 60 * 8
